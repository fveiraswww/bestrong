version: "3.9"
services:
  db:
    image: postgres
    volumes:
      - postgres-db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=medusa_db
      - POSTGRES_USER=fveiras
      - POSTGRES_PASSWORD=00xs3aY2W8yjJQ
    ports:
      - 5432:5432
volumes:
  postgres-db: